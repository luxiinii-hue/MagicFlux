{
  "name": "magic-flux",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/types",
    "packages/engine",
    "packages/cards",
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.1"
  }
}
